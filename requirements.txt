# CRITs core dependencies — Python 3.14, pinned to current (June 2026) releases.
# Native/optional analysis libraries live in requirements-optional.txt.

# --- Web framework + database ---
Django>=5.2,<5.3                 # 5.2 LTS
mongoengine>=0.29,<0.30
pymongo>=4.6,<5

# --- Core libraries ---
Pillow>=11,<12
requests>=2.32,<3
lxml>=6,<7
defusedxml>=0.7
python-magic>=0.4.27
python-dateutil>=2.9
pytz>=2025.1
PyYAML>=6,<7
pyparsing>=3,<4
packaging>=24
chardet>=5,<6
olefile>=0.47
qrcode>=8,<9
pycryptodome>=3.21         # provides the Crypto.* namespace (TOTP secret crypto)

# --- REST API (django-tastypie) ---
# Enabled only when settings.ENABLE_API is True. tastypie-mongoengine is
# unmaintained / incompatible with modern mongoengine, so the API resources
# are not wired up yet. Uncomment once they are ported.
# django-tastypie>=0.14
# python-mimeparse>=1.6
